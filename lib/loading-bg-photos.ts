/** Curated loader backdrop — current debut photos for a fast first paint. */
export const LOADING_BG_PHOTOS = [
  "/desktop-background/new-debut (1).webp",
  "/mobile-background/new-debut (1).webp",
].map((src) => encodeURI(src))
